import { HighlightNumberDirective } from './highlight-number.directive';

describe('HighlightNumberDirective', () => {
  it('should create an instance', () => {
    // const directive = new HighlightNumberDirective();
    // expect(directive).toBeTruthy();
  });
});
